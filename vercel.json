{
  "version": 2,
  "buildCommand": "NODE_ENV=production npm run build",
  "outputDirectory": "dist/public",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/server" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
